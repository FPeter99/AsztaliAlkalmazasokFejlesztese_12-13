<Window x:Class="Eletjatek.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Eletjatek.ViewModel"
        xmlns:local="clr-namespace:Eletjatek"
        mc:Ignorable="d"
        Title="Életjáték álláskészítő" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <DockPanel>
        <StackPanel Margin="0,20,0,0"  DockPanel.Dock="Top" Orientation="Horizontal">
            <Label Content="Mátrix mérete [sor x oszlop]:"/>
            <ComboBox SelectedValue="{Binding Rows, Mode=TwoWay}" SelectedIndex="15" ItemsSource="{Binding Meretek}" Margin="10,0,0,0"/>
            <ComboBox SelectedValue="{Binding Cols, Mode=TwoWay}" SelectedIndex="15" ItemsSource="{Binding Meretek}" Margin="10,0,0,0"/>

        </StackPanel>

        <StackPanel Margin="0,20,0,0" DockPanel.Dock="Top" Orientation="Horizontal">
            <Button Content="Üres mátrix létrehozása" Command="{Binding CreateNew}" Margin="5,0,0,0"/>
            <Button Content="Állás mentése" Command="{Binding Save}" Margin="15,0,0,0"/>
        </StackPanel>
        <UniformGrid Name="ChekBoxGrid" Margin="20,0,20,20"/>
    </DockPanel>
</Window>
